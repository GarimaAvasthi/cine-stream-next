

Below are example prompts that reflect the process and logic for building the Cine-Stream App based on the requirements:

## Prompts

* "How do I move my Week 8 React movie application components and styles into the Next.js App Router structure?"
* "Create file-based routing for my application. I need the main homepage at `/` and a movie details page inside `app/movie/`."
* "How do I fetch the 'Popular Movies' list from the TMDB API inside a Next.js 15 Server Component without using `useEffect` for the initial load?"
* "Implement Next.js dynamic SEO Metadata using the `generateMetadata` function inside my dynamic movie route to populate the `<title>` and `<meta name='description'>` tags based on fetched TMDB API movie details."
* "Adjust the UI layout in `app/ui/HomePage.tsx` to correctly expand the Popular Picks section and remove legacy Match My Mood components."
